apiVersion: v1
kind: Service
metadata:
  name: sonarqube
  namespace: app
spec:
  selector:
    app: sonarqube

  ports:
    - name: http
      protocol: TCP
      port: 9000
      targetPort: 9000

  type: ClusterIP