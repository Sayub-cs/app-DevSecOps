apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: app
spec:
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 9090
      targetPort: 9090
  type: LoadBalancer