apiVersion: v1
kind: Service
metadata:
  name: business-service
  namespace: app
spec:
  selector:
    app: business-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP